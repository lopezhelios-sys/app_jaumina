/**
 * Tipos generados desde la base de datos de Supabase
 * TODO: Generar automáticamente con supabase gen types
 */

export interface Database {
  public: {
    Tables: {
      // Tipos pendientes de generación
    };
    Views: {
      // Vistas pendientes de generación
    };
    Functions: {
      // Funciones pendientes de generación
    };
  };
}
